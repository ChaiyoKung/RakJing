import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  roundness: 14,
  colors: {
    ...DefaultTheme.colors,
    primary: "#DD0426",
    accent: "#FFFFCE",
  },
};

export default theme;
